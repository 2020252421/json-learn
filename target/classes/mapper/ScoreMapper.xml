<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.colin.mapper.ScoreMapper">
    <!--collection中的map是dao层传入的map参数，item中的value表示map中的值，index中的key表示map中键-->
    <!--foreach进行循环map取出里面的key和value-->
    <!--foreach标签属性： item，index，collection，open，separator，close。
    item：每个遍历生成的对象。必填
    index：指定一个名字，用于表示在迭代过程中，当前的迭代次数。从零开始递增。
    collection：输入对象中的集合属性。必填
    open：开始遍历时拼接的字符串，
    separator：遍历时两个对象之间需要设置的分隔符。必填
    close:结束遍历时拼接的字符串-->
    <insert id="insertScore" parameterType="java.util.Map" >

      insert into score
      <foreach item="value" collection="columnMap" index="key"    separator=",">
           ${key}
       </foreach>
      values
       <foreach item="value" collection="columnMap" index="key"    separator=",">
           #{value}
       </foreach>
    </insert>
</mapper>